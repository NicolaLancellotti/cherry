//===--- DiagnosticsParse.def - Diagnostics Text ----------------*- C++ -*-===//
//
// This source file is part of the Cherry open source project
// See TODO for license information
//
//===----------------------------------------------------------------------===//

// ___________________________________________________________________________
// Parse Declarations

ERROR(expected_fun_struct, "expected `fun` or `struct`")

// prototype
ERROR(expected_id,
    "expected identifier")
ERROR(expected_colon,
     "expected `:`")
ERROR(expected_type,
     "expected type")
ERROR(expected_l_paren_in_arg_list,
    "expected `(` in argument list of function declaration")


// Parse Types
ERROR(expected_l_brace,
    "expected `{`")

ERROR(expected_comma_or_r_brace,
    "expected `,` or `}`")

// ___________________________________________________________________________
// Parse Expressions

ERROR(expected_expr,
    "expected expression")

ERROR(integer_literal_overflows,
    "integer literal overflows")

// function body
ERROR(expected_l_brace_func_body,
    "expected `{` in body of function declaration")
ERROR(expected_r_brace_func_body,
    "expected `}` at end of brace statement")
ERROR(expected_semi,
    "expected `;` at end of declaration")

// function call
ERROR(expected_comma_or_r_paren_arg_list,
    "expected `,` or `)` in argument list")

// ___________________________________________________________________________
#undef ERROR
